import type { NextConfig } from 'next';

// Use the default Next.js webpack/optimization config.
// Customizing `config.optimization` (e.g. `runtimeChunk`) can conflict
// with the App Router and cause issues like ChunkLoadError.
const nextConfig: NextConfig = {};

export default nextConfig;
